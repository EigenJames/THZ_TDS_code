name: thz-tds-ai
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - numpy
  - pandas
  - scipy
  - scikit-learn
  - pip
  - pip:
      - plotly
      - kaleido
      - requests
      - tqdm
      - joblib
      - pytest
      - black
      - isort
      - ruff
      - pre-commit
      - papermill
